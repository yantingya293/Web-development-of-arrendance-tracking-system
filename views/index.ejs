<%- include('partials/header', { title, active: 'home' }) %>

    <section class="hero">
      <h1>学习打卡网页</h1>
      <p class="sub">单人任务打卡 · 双人协作监督 · 全员成果展示</p>
      <% if (currentUser) { %>
        <p class="muted">你好，<strong><%= currentUser.nickname %></strong>！今天也要坚持打卡哦（Day 3 / 21，用户系统已上线）。</p>
      <% } else { %>
        <p class="muted">脚手架已就绪，注册并登录后即可开始使用（Day 3 / 21）。</p>
        <div class="hero-actions">
          <a href="/register" class="btn btn-primary">立即注册</a>
          <a href="/login" class="btn btn-outline">登录</a>
        </div>
      <% } %>
    </section>
    <!-- Day4 起填充首页四大板块：左上单人任务 / 右上双人任务 / 中下左打卡表单 / 中下右实时动态 -->

<%- include('partials/footer') %>
